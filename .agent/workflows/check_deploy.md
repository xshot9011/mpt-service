---
description: Run Django deploy check
---

# Check Deploy

When the user asks to "check deploy" or uses the `/check_deploy` command, run the following command to check if the Django project is ready for deployment:

// turbo
```bash
python3 manage.py check --deploy
```
